# Pipeline de Procesamiento de PDFs - ResidusIA Pro
# ===================================================

# PDF
pdfplumber==0.11.0          # extracción texto + tablas de PDFs digitales
pdf2image==1.17.0           # conversión PDF a imagen para OCR
pytesseract==0.3.13         # OCR (requiere Tesseract instalado en el sistema)
pikepdf==9.0.0              # manejo de PDFs encriptados/protegidos
Pillow==10.4.0              # manipulación de imágenes para preprocesamiento OCR

# IA
anthropic==0.34.0           # Claude para clasificación y extracción de metadatos
openai==1.45.0              # embeddings text-embedding-3-large
langchain-core==0.3.23      # tipos base compartidos con LangGraph
langgraph==0.2.76           # orquestación multi-agente

# Base de datos
supabase==2.7.0             # cliente Supabase async

# Excel
openpyxl==3.1.5             # lectura de archivos Excel (.xlsx)
pandas==2.2.2               # procesamiento tabular de datos Excel/CSV

# Word (DOCX)
python-docx==1.1.2          # lectura de archivos Word (.docx)

# Utilidades
python-dotenv==1.0.1
aiofiles==24.1.0

# Google Drive
google-api-python-client==2.108.0  # Google Drive API v3
google-auth-httplib2==0.2.0        # auth transport
google-auth-oauthlib==1.2.0        # OAuth2 flow

# API Server
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
